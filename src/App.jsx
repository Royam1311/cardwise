import React,{useEffect,useMemo,useState} from 'react';
import {Search,CreditCard,LogOut,Plus,Trash2,ShieldCheck,Sparkles,User,Tag,Settings,Store,Package,Percent,X,Save,RefreshCw} from 'lucide-react';
import {supabase,configured} from './supabase';

const FALLBACK_CARDS=[['visa','Visa רגיל'],['tav','תו הזהב'],['htz','הייטקזון'],['max','MAX'],['isracard','ישראכרט'],['haver','חבר'],['behatsdaa','בהצדעה']];
const DEMO=[
 {id:'d1',product_name:'Apple AirPods Pro 2 USB-C',sku:'MTJV3ZM/A',category:'אוזניות',offers:[['KSP',999,0,'htz','12% הנחה'],['Ivory',1049,0,'visa','₪30 החזר'],['BUG',1149,0,'htz','20% הנחה']]},
 {id:'d2',product_name:'Ninja Woodfire OO101',sku:'OO101',category:'מטבח וגינה',offers:[['שקם אלקטריק',1690,0,'htz','18% הנחה'],['LastPrice',1599,39,'visa','₪50 החזר'],['ACE',1790,0,'tav','20% הנחה']]},
 {id:'d3',product_name:'Sony PlayStation 5 Slim',sku:'CFI-2016A',category:'גיימינג',offers:[['KSP',2199,0,'htz','10% הנחה'],['Ivory',2149,0,null,'ללא הטבה'],['BUG',2299,0,'tav','12% הנחה']]}
];
const money=n=>new Intl.NumberFormat('he-IL',{style:'currency',currency:'ILS',maximumFractionDigits:0}).format(Number(n||0));
function effective(price,note){const pct=Number(note?.match(/(\d+)%/)?.[1]||0),cash=Number(note?.match(/₪(\d+)/)?.[1]||0);return Math.max(0,price*(1-pct/100)-cash)}

function Auth({onDemo}){const[mode,setMode]=useState('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setMsg('');if(!configured){setMsg('חסרים משתני חיבור ל-Supabase.');return}setBusy(true);const {error}=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});setBusy(false);setMsg(error?error.message:(mode==='login'?'התחברת בהצלחה':'נשלח אליך אימייל לאישור.'))}return <main className="auth"><section className="brand-panel"><div className="logo"><CreditCard/> CardWise</div><h1>המחיר שמתאים דווקא לך</h1><p>השוואת מחירים, משלוחים והטבות לפי הכרטיסים והמועדונים שלך.</p><div className="feature"><ShieldCheck/> אין צורך להזין מספר כרטיס או CVV</div></section><section className="auth-card"><h2>{mode==='login'?'כניסה לחשבון':'יצירת חשבון'}</h2><form onSubmit={submit}><label>אימייל<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>סיסמה<input type="password" minLength="6" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary" disabled={busy}>{busy?'טוען...':mode==='login'?'כניסה':'הרשמה'}</button></form>{msg&&<div className="notice">{msg}</div>}<button className="link" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'אין לך חשבון? הרשמה':'כבר נרשמת? כניסה'}</button><button className="demo" onClick={onDemo}>כניסה לגרסת הדגמה</button></section></main>}

function Modal({title,onClose,children}){return <div className="modal-bg" onMouseDown={onClose}><div className="modal" onMouseDown={e=>e.stopPropagation()}><button className="close" onClick={onClose}><X/></button><h2>{title}</h2>{children}</div></div>}
function Field({label,...p}){return <label className="field">{label}<input {...p}/></label>}

function Admin({onChanged}){const[section,setSection]=useState('products'),[items,setItems]=useState([]),[stores,setStores]=useState([]),[cards,setCards]=useState([]),[open,setOpen]=useState(false),[form,setForm]=useState({}),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');
 const cfg={products:{title:'מוצרים',icon:Package,table:'products'},stores:{title:'חנויות',icon:Store,table:'stores'},benefits:{title:'הטבות',icon:Percent,table:'benefit_rules'}};
 async function load(){setMsg('');const q=section==='benefits'?supabase.from('benefit_rules').select('*,stores(store_name),card_programs(display_name)').order('created_at',{ascending:false}):supabase.from(cfg[section].table).select('*').order(section==='products'?'product_name':'store_name');const {data,error}=await q;if(error)setMsg(error.message);setItems(data||[]);const [{data:s},{data:c}]=await Promise.all([supabase.from('stores').select('id,store_name').eq('active',true),supabase.from('card_programs').select('id,display_name').eq('active',true)]);setStores(s||[]);setCards(c||[])}
 useEffect(()=>{load()},[section]);
 async function save(e){e.preventDefault();setBusy(true);setMsg('');let payload;if(section==='products')payload={product_name:form.product_name,sku:form.sku||null,category:form.category||null,brand:form.brand||null,model:form.model||null,active:true};if(section==='stores')payload={store_name:form.store_name,slug:form.slug,website:form.website||null,active:true};if(section==='benefits')payload={card_program_id:form.card_program_id,store_id:form.store_id||null,category:form.category||null,benefit_type:form.benefit_type,benefit_value:Number(form.benefit_value),terms:form.terms||null,starts_at:form.starts_at||null,ends_at:form.ends_at||null,active:true};const {error}=await supabase.from(cfg[section].table).insert(payload);setBusy(false);if(error){setMsg(error.message);return}setOpen(false);setForm({});await load();onChanged?.()}
 async function remove(id){if(!window.confirm('למחוק את הרשומה?'))return;const {error}=await supabase.from(cfg[section].table).delete().eq('id',id);if(error)setMsg(error.message);else load()}
 const CfgIcon=cfg[section].icon;
 return <section className="page admin"><div className="page-title"><div><h1>פאנל ניהול</h1><p>ניהול הנתונים של CardWise ללא שינוי קוד.</p></div><Settings/></div><div className="admin-tabs">{Object.entries(cfg).map(([k,v])=>{const I=v.icon;return <button className={section===k?'active':''} onClick={()=>setSection(k)} key={k}><I/>{v.title}</button>})}</div><div className="admin-toolbar"><h2><CfgIcon/>{cfg[section].title}</h2><div><button className="secondary" onClick={load}><RefreshCw/>רענון</button><button className="primary" onClick={()=>{setForm({benefit_type:'percent'});setOpen(true)}}><Plus/>הוספה</button></div></div>{msg&&<div className="notice">{msg}</div>}<div className="table-wrap"><table><thead><tr>{section==='products'&&<><th>שם</th><th>מק״ט</th><th>קטגוריה</th><th>מותג</th></>}{section==='stores'&&<><th>חנות</th><th>מזהה</th><th>אתר</th></>}{section==='benefits'&&<><th>כרטיס/מועדון</th><th>חנות</th><th>סוג</th><th>ערך</th></>}<th></th></tr></thead><tbody>{items.map(x=><tr key={x.id}>{section==='products'&&<><td>{x.product_name}</td><td>{x.sku||'-'}</td><td>{x.category||'-'}</td><td>{x.brand||'-'}</td></>}{section==='stores'&&<><td>{x.store_name}</td><td>{x.slug}</td><td>{x.website||'-'}</td></>}{section==='benefits'&&<><td>{x.card_programs?.display_name}</td><td>{x.stores?.store_name||x.category||'כללי'}</td><td>{x.benefit_type}</td><td>{x.benefit_type==='percent'?x.benefit_value+'%':money(x.benefit_value)}</td></>}<td><button className="danger" onClick={()=>remove(x.id)}><Trash2/></button></td></tr>)}{!items.length&&<tr><td colSpan="6" className="empty">אין עדיין רשומות. לחץ על הוספה.</td></tr>}</tbody></table></div>{open&&<Modal title={'הוספת '+cfg[section].title.slice(0,-2)} onClose={()=>setOpen(false)}><form className="admin-form" onSubmit={save}>{section==='products'&&<><Field label="שם מוצר *" required value={form.product_name||''} onChange={e=>setForm({...form,product_name:e.target.value})}/><Field label="מק״ט" value={form.sku||''} onChange={e=>setForm({...form,sku:e.target.value})}/><Field label="קטגוריה" value={form.category||''} onChange={e=>setForm({...form,category:e.target.value})}/><Field label="מותג" value={form.brand||''} onChange={e=>setForm({...form,brand:e.target.value})}/><Field label="דגם" value={form.model||''} onChange={e=>setForm({...form,model:e.target.value})}/></>}{section==='stores'&&<><Field label="שם חנות *" required value={form.store_name||''} onChange={e=>setForm({...form,store_name:e.target.value})}/><Field label="מזהה באנגלית *" required placeholder="ksp" value={form.slug||''} onChange={e=>setForm({...form,slug:e.target.value.toLowerCase().replace(/\s/g,'-')})}/><Field label="כתובת אתר" type="url" value={form.website||''} onChange={e=>setForm({...form,website:e.target.value})}/></>}{section==='benefits'&&<><label className="field">כרטיס או מועדון *<select required value={form.card_program_id||''} onChange={e=>setForm({...form,card_program_id:e.target.value})}><option value="">בחר</option>{cards.map(c=><option key={c.id} value={c.id}>{c.display_name}</option>)}</select></label><label className="field">חנות<select value={form.store_id||''} onChange={e=>setForm({...form,store_id:e.target.value})}><option value="">בחר או הגדר קטגוריה</option>{stores.map(s=><option key={s.id} value={s.id}>{s.store_name}</option>)}</select></label><Field label="קטגוריה, אם אין חנות" value={form.category||''} onChange={e=>setForm({...form,category:e.target.value})}/><label className="field">סוג הטבה<select value={form.benefit_type||'percent'} onChange={e=>setForm({...form,benefit_type:e.target.value})}><option value="percent">אחוז הנחה</option><option value="fixed">הנחה קבועה</option><option value="cashback">Cashback</option><option value="special_price">מחיר מיוחד</option></select></label><Field label="ערך *" required type="number" min="0" step="0.01" value={form.benefit_value||''} onChange={e=>setForm({...form,benefit_value:e.target.value})}/><Field label="תנאים" value={form.terms||''} onChange={e=>setForm({...form,terms:e.target.value})}/></>}<button className="primary" disabled={busy}><Save/>{busy?'שומר...':'שמור'}</button></form></Modal>}</section>}

export default function App(){const[session,setSession]=useState(null),[demo,setDemo]=useState(false),[cards,setCards]=useState([]),[cardCatalog,setCardCatalog]=useState(FALLBACK_CARDS),[query,setQuery]=useState(''),[tab,setTab]=useState('search'),[loading,setLoading]=useState(true),[isAdmin,setIsAdmin]=useState(false);
 useEffect(()=>{if(!configured){setLoading(false);return}supabase.auth.getSession().then(({data})=>{setSession(data.session);setLoading(false)});const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>subscription.unsubscribe()},[]);
 useEffect(()=>{if(session){loadUser();loadCatalog()}else if(demo)setCards(['visa','tav','htz'])},[session,demo]);
 async function loadUser(){const [{data:c},{data:p}]=await Promise.all([supabase.from('cards').select('card_type').order('created_at'),supabase.from('profiles').select('role').eq('id',session.user.id).maybeSingle()]);setCards((c||[]).map(x=>x.card_type));setIsAdmin(p?.role==='admin')}
 async function loadCatalog(){const{data}=await supabase.from('card_programs').select('code,display_name').eq('active',true).order('display_name');if(data?.length)setCardCatalog(data.map(x=>[x.code,x.display_name]))}
 async function toggleCard(code){if(demo){setCards(x=>x.includes(code)?x.filter(c=>c!==code):[...x,code]);return}if(cards.includes(code))await supabase.from('cards').delete().eq('user_id',session.user.id).eq('card_type',code);else await supabase.from('cards').insert({user_id:session.user.id,card_type:code,card_name:cardCatalog.find(x=>x[0]===code)?.[1]||code});loadUser()}
 const product=useMemo(()=>{const q=query.trim().toLowerCase();return DEMO.find(p=>(p.product_name+' '+p.sku+' '+p.category).toLowerCase().includes(q))||DEMO[0]},[query]);const offers=useMemo(()=>product.offers.map(([store,price,shipping,card,note])=>{const eligible=card&&cards.includes(card),final=eligible?effective(price,note):price;return{store,price,shipping,note:eligible?note:'ללא הטבה זמינה',total:final+shipping,final}}).sort((a,b)=>a.total-b.total),[product,cards]);
 if(loading)return <div className="center">טוען...</div>;if(!session&&!demo)return <Auth onDemo={()=>setDemo(true)}/>;const email=session?.user?.email||'משתמש הדגמה';
 return <div dir="rtl"><header><div className="logo dark"><CreditCard/>CardWise</div><nav><button className={tab==='search'?'active':''} onClick={()=>setTab('search')}><Search/>חיפוש</button><button className={tab==='cards'?'active':''} onClick={()=>setTab('cards')}><CreditCard/>הכרטיסים שלי</button>{isAdmin&&<button className={tab==='admin'?'active':''} onClick={()=>setTab('admin')}><Settings/>ניהול</button>}</nav><div className="user"><User/>{email}<button onClick={()=>session?supabase.auth.signOut():setDemo(false)}><LogOut/></button></div></header>{demo&&<div className="demo-banner">מצב הדגמה: הנתונים אינם אמיתיים.</div>}{tab==='admin'?<Admin/>:tab==='cards'?<section className="page"><div className="page-title"><div><h1>הכרטיסים והמועדונים שלי</h1><p>בחר רק את סוגי הכרטיסים. אין להזין מספר כרטיס.</p></div><CreditCard/></div><div className="card-grid">{cardCatalog.map(([code,name])=><button key={code} onClick={()=>toggleCard(code)} className={'credit-card '+(cards.includes(code)?'selected':'')}><span>{name}</span><small>{cards.includes(code)?'נוסף לחשבון':'לחץ להוספה'}</small>{cards.includes(code)?<Trash2/>:<Plus/>}</button>)}</div></section>:<><section className="hero"><span><Sparkles/>השוואת המחיר האישי שלך</span><h1>מוצאים את העסקה שבאמת<br/>הכי משתלמת עבורך</h1><form onSubmit={e=>e.preventDefault()}><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="לדוגמה: AirPods, OO101 או PS5"/><button className="primary">השווה מחירים</button></form></section><section className="page results"><aside><div className="product-icon">{product.category==='אוזניות'?'🎧':product.category==='גיימינג'?'🎮':'🔥'}</div><span className="category">{product.category}</span><h2>{product.product_name}</h2><p>מק״ט: {product.sku}</p><div className="summary"><Tag/>הכרטיסים הפעילים: {cards.length}</div></aside><main><h2>המחירים שלך אחרי ההטבות</h2>{offers.map((o,i)=><article className={i===0?'best':''} key={o.store}>{i===0&&<b className="best-label">העסקה המשתלמת ביותר עבורך</b>}<div><h3>{o.store}</h3><small><ShieldCheck/>חנות לדוגמה</small></div><div><del>{money(o.price)}</del><strong>{money(o.final)}</strong><span>{o.note}</span><p>משלוח: {o.shipping?money(o.shipping):'חינם'} | סה״כ: <b>{money(o.total)}</b></p></div><button>לרכישה</button></article>)}<div className="warning">נתוני המוצרים, המחירים וההטבות עדיין נתוני הדגמה.</div></main></section></>}</div>}
